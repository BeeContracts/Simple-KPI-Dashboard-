# Simple-KPI-Dashboard-
Business KPI Dashboard is a Python desktop app that loads CSV data, lets users choose key business columns, and generates useful metrics like total records, total value, average value, and category breakdowns with exportable JSON/CSV summaries.
